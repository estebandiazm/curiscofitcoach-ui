import React from 'react'

const style = {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#343a40',
    height: '100vh',
    color: '#fff'
}

const Dashboard = () => {
    return (
        <div style={style}>
            <h1>Building!!</h1>
        </div>
    )
}

export default Dashboard